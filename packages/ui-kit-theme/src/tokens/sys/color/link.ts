import { RefPalette } from '../../ref'

export const SysColorLink = {
  default: {
    light: RefPalette.lime['700'],
    dark: RefPalette.lime['400'],
  },
  pressed: {
    light: RefPalette.lime['800'],
    dark: RefPalette.lime['300'],
  },
} as const
