{
  "name": "@teleskop-labs/prettier-config",
  "version": "0.0.0",
  "private": true,
  "description": "prettier-config",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules"
  }
}
